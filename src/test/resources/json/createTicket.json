{
  "queue": "${queue}",
  "due_date": "${due_date}",
  "assigned_to": "${assigned_to}",
  "status": "${status}",
  "title": "${title}",
  "description": "${description}",
  "priority": "${priority}",
  "submitter_email": "${submitter_email}"

}
